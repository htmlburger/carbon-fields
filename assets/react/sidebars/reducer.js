/**
 * The reducer that handles the `sidebars` branch.
 *
 * @param  {Object} [state]
 * @param  {Object} [action]
 * @return {Object}
 */
export default function(state = {}, action = {}) {
	return state;
}
