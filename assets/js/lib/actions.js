/**
 * The external dependencies.
 */
import { createAction } from 'redux-actions';

/**
 * Everything is loaded and rendered.
 *
 * @return {Object}
 */
export const ready = createAction('lib/READY');
