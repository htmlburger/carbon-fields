/**
 * Internal dependencies.
 */
import reducer from '@gutenberg/store/reducer';
import * as actions from '@gutenberg/store/actions';
import * as selectors from '@gutenberg/store/selectors';

export default {
	reducer,
	actions,
	selectors
};
